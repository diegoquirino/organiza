<h1 class="mt-5 mb-3"><i class="far fa-clipboard"></i> Dados Básicos do Planejamento</h1>

<% if (errors) { %>
    <div class="alert alert-danger" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
        <h4 class="alert-heading">Atenção!</h4>
        <hr>
        <ul>
            <% errors.forEach((error) => { %>
                <li><%= error.msg %></li>
            <% }) %>
        </ul>
    </div>
<% } %>

<p class="mb-5">Preencha os campos do formulário abaixo, a fim de gerar o arquivo base para o Planejamento das atividades de Testes:</p>

<div class="row">
    <div class="col-12 offset-0">
        <div class="card">
            <div class="card-body">
                <form method="post" action="download">
                    <div class="form-group">
                        <label for="sistema">Nome do Sistema:</label>
                        <input type="text" class="form-control" id="sistema" name="sistema" aria-describedby="sistemaHelp" placeholder="Inserir o nome do sistema" value="<%= baseplan.sistema %>">
                        <small id="sistemaHelp" class="form-text text-muted">Ex: RGP-Frequência, SISCOM-w, ADM-Eletrônico, etc.</small>
                    </div>
                    <div class="form-group">
                        <label for="versao">Versão:</label>
                        <input type="text" class="form-control" id="versao" name="versao" aria-describedby="versaoHelp" placeholder="Inserir o número da versão" value="<%= baseplan.versao %>">
                        <small id="versaoHelp" class="form-text text-muted">Ex: 1.0, 2.0.1, 3.2.0.1, etc.</small>
                    </div>
                    <div class="form-group">
                        <label for="baseline">Baseline:</label>
                        <input type="text" class="form-control" id="baseline" name="baseline" aria-describedby="baselineHelp" placeholder="Inserir o número da baseline" value="<%= baseplan.baseline %>">
                        <small id="baselineHelp" class="form-text text-muted">Ex: 1, 2, 3, etc.</small>
                    </div>
                    <button type="submit" class="btn btn-primary mt-5">Gerar Arquivo</button>
                    <button type="reset" class="btn btn-secondary mt-5" onclick="limpar()">Limpar</button>
                </form>
            </div>
        </div>
    </div>
</div>